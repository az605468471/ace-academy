# Ark Academy（王牌学院）部署说明

> 目的：把 DApp 前端免费托管上线，并绑定你已购买的域名 `ark-academy.biz`，让用户能通过正式域名访问。

---

## 一、当前已完成的部分

| 项目 | 状态 |
|------|------|
| 前端全部页面（首页/登录/学习/课程库/个人/学分/考试/推广/交易/管理后台/AI规划师） | ✅ 已完成（纯 HTML/CSS/JS，11个页面） |
| 6 个智能合约（ACEToken、CoursePurchase、ReferralRewards、TokenLock、MarketManager、CertificateNFT） | ✅ 已完成 + 安全审计 + 编译通过 |
| 域名 | ✅ 已购买激活：`ark-academy.biz`（有效期至 2027-08-03） |
| 服务器 | ❌ 不需要买，用免费托管 |
| 钱包/合约链上连接 | ⚠️ 目前为「模拟」，合约尚未部署主网 |

---

## 二、预览链接（先看效果）

> 当前前端已在我这台部署环境上线，可先打开看效果：

**https://0b98bfcc.strike-agent.com**

可访问页面示例：
- 首页：`/index.html`
- 登录：`/login.html`
- 学习中心：`/learning.html`
- 课程库：`/course-library.html`
- 管理后台：`/admin.html`

> ⚠️ 注意：这是**临时演示域名**，用于预览效果和测试，不是正式生产地址。正式上线需用你自己的域名 `ark-academy.biz`。

---

## 三、正式上线方案（绑定你的域名）

推荐用免费托管平台之一，把前端放上去，再让域名指向它。

### 方案1：Cloudflare Pages（推荐）
优点：免费、速度快、CDN全球、自带DNS管理（域名也是在这管理更方便）。

### 方案2：Vercel（推荐）
优点：免费、部署简单、全球节点。

### 方案3：IPFS / 其他静态托管

---

## 四、绑定域名的步骤（以 Cloudflare 为例）

### 步骤1：把域名转入或添加到 Cloudflare
1. 注册/登录 Cloudflare（cloudflare.com）
2. Add a site → 输入 `ark-academy.biz`
3. 按提示把 NS（Nameserver）改成 Cloudflare 提供的两个地址
4. 回到 VPS Core（你域名注册处），改 NS 生效（可能要等几分钟到几小时）

### 步骤2：部署前端到 Cloudflare Pages
1. Cloudflare → Pages → Create project
2. 上传前端文件（所有 .html/.js/.css/images）
3. 等待构建完成，得到一个 `xxx.pages.dev` 的临时域名

### 步骤3：绑定正式域名
1. Cloudflare Pages → 你的项目 → Custom domains
2. 绑定 `ark-academy.biz`
3. 等待 DNS 生效（几分钟到几小时）

### 步骤4：验证
- 浏览器打开 `https://ark-academy.biz`，能打开首页即成功
- 建议开启 Cloudflare 的 SSL/TLS（免费），让网站走 HTTPS（解决之前域名显示的红锁）

---

## 五、前后端文件清单（部署时上传这些）

以下是需要托管的前端文件（放在 ark-academy 目录下）：

```
index.html
login.html
learning.html
course-library.html
profile.html
credits.html
exam.html
promote.html
trade.html
admin.html
ai-planner.html
frame.js
i18n.js
courses-data.js
images/            ← 整个图片文件夹
```

> 6 个 `.sol` 合约**不需要**上传到前端托管，它们是部署到 BSC 链上的（后续单独部署）。

---

## 六、后续事项（合约部署，另行进行）

前端上线后，下一步是：
1. 购买一点 BNB（BSC 链上的 gas 费，约 5-10 美元足够部署全部合约）
2. 用钱包部署 6 个合约到 BSC 主网
3. 把部署得到的合约地址填进前端（替换当前的"模拟"交互）
4. 实现真实链上购买课程、发放证书、推荐奖励等功能

此部分需要：你的 BSC 钱包地址、一点 BNB、以及确认代币经济参数。

---

## 七、关键提醒

- 前端免费托管**不花钱**，域名你已经买了（约17 USD/年）
- 合约部署需要一点 BNB（gas），但金额很小
- **不要买传统服务器**，纯静态前端用免费托管即可
- 有任何报错、看不懂的界面，截图发给 MasterD 协助

---

*Generated by MasterD · Ark Academy DApp 部署指引*
