# 合约部署 · 钱包与资金安全方案

> 更新：2026-08-03
> 说明：本方案用于 ACE Academy（王牌学院）6 个合约的 BSC 主网部署。核心目标是**资金与部署分离**，确保 1000 万 ACE 等大额资产始终控制在用户本人可信钱包中，部署工具只接触少量 BNB（gas）。

---

## 一、资金安全核心逻辑（用户拍板）

1. **1000 万 ACE 绝对不能放在"给工具部署"的钱包里** —— 因为部署要用到该钱包私钥，经手工具会带来泄露风险。
2. **部署钱包**：只放少量 BNB（几十美元，仅用于 gas 费），即使泄露损失也极小。
3. **资金钱包**：由用户本人掌控，持有 1000 万 ACE、USDT 等大额资产，**任何工具都不接触其私钥**。
4. **部署动作由 AI/工具完成**。用户提供"部署钱包①"的 BNB 与授权（签名权限），但大额 ACE 资产放在"资金钱包②"。

---

## 二、钱包用途表（共需 5 个钱包）

| # | 钱包角色 | 用途 | 是否交由工具 | 放什么资产 | 私钥归属 |
|---|---------|------|:---:|-----------|---------|
| ① | **部署钱包 Owner** | 部署 6 个合约、传参、初始化、加底池等链上操作 | **本钱包给 AI 使用** | 仅放 BNB（约 0.1 BNB 够用） | 用户创建后提供地址，BNB 由用户充值 |
| ② | **资金钱包**（用户个人分配） | 接收 **25 万 ACE** 自由支配；作为 ACE 合约 owner / 大额资产持有 | ❌ 绝不接触 | 1000万 ACE 分配、25万自由支配、USDT | **用户本人掌控，任何人/工具不得触碰** |
| ③ | **公益钱包 charityWallet** | 收取买卖手续费中的公益部分 | 只填地址 | 无（收款用） | 用户创建 |
| ④ | **平台钱包 platformWallet** | 收取平台运营费 | 只填地址 | 无（收款用） | 用户创建 |
| ⑤ | **底池钱包 poolWallet** | 存放底池流动性，定期注入 PancakeSwap | 只填地址 | 无（后续加底池时用） | 用户创建 |

> 注：③④⑤ 可以是同一个地址，但**强烈建议分开**，便于对账与分散风险。

### 固定的链上合约地址（无需用户创建）
| 资产 | BSC 地址 |
|------|---------|
| USDT（BSC 主网） | `0x55d398326f99059fF775485246999027B3197955` |
| ACE | 部署后自动生成 |

---

## 三、6 个合约部署时需要的钱包参数

| 合约 | 部署需要的地址参数 | 参数来源 |
|------|------------------|---------|
| 1_ACEToken | `_charityWallet` | 公益钱包③ |
| 2_CoursePurchase | `_aceToken`(ACE) + `_usdt` + `_platformWallet` + `_charityWallet` + `_poolWallet` | ACE合约地址 + USDT + 平台③公益④池⑤ |
| 3_ReferralRewards | `_aceToken` | ACE 合约地址 |
| 4_TokenLock | `_aceToken` | ACE 合约地址 |
| 5_MarketManager | `_aceToken` + `_usdt` + `_platformWallet` + `_charityWallet` | ACE + USDT + 平台③公益④ |
| 6_CertificateNFT | `_aceToken` | ACE 合约地址 |

---

## 四、关键决策点：1000 万 ACE 在哪个钱包（待最终拍板）

当前 ACEToken 构造函数把**全部 1000 万 ACE mint 给 `msg.sender`（部署者）**。

需在以下方案中选择，确保 ACE 归用户而非工具：

### 方案 A：合约支持"指定 owner"（推荐，最安全）
- 修改 ACEToken / 各合约，支持在部署时指定 `owner`（= 资金钱包②）。
- 即使用部署钱包①发起部署，**owner / 1000万 ACE 也直接归钱包②**。
- 需调整合约 constructor 增加 owner 参数（对部署钱包①只需 BNB，无资产风险）。

### 方案 B：部署后立刻转走（不改合约，靠流程）
- 用部署钱包①部署 ACEToken，部署完**立刻把 1000 万 ACE 全部转到钱包②**。
- 之后部署钱包①只留 BNB。
- 快，无需改合约，但中间有一小段 ACE 在部署钱包①里的时间窗口。

> ⚠️ 部署前需确认采用 A 或 B。若选 A 需改合约并重新审计编译。

---

## 五、ACE 代币分配（总量 1000 万）

| 分配 | 数量 | 占比 | 说明 |
|------|------|------|------|
| 课程赠送池 | 400万 | 40% | 学员买课赠送 |
| 大使赠送池 | 100万 | 10% | 大使交 1000U 赠送 |
| 团队奖励池 | 100万 | 10% | 团队级别分红 |
| 底池流动性 | 200万 | 20% | 初始+持续注入 |
| 团队锁定 | 150万 | 15% | 团队持有，锁定1年 |
| 推广预留 | 50万 | 5% | 初期推广补贴、排名奖 |
| **合计** | **1000万** | 100% | —— |

> 用户个人自由支配 **25 万 ACE**：用"部署后直接转账"到**资金钱包②**，不进合约函数，不做锁定。（此前已确认方案 A 直接转账）

---

## 六、BNB 需求量估算

| 合约 | 预估部署 gas |
|------|-------------|
| ACEToken | ~180万 |
| CoursePurchase（最大） | ~350万 |
| ReferralRewards | ~150万 |
| TokenLock | ~120万 |
| MarketManager | ~200万 |
| CertificateNFT | ~250万 |
| **合计** | **约 1250万 gas** |

按 BSC gas 5 gwei 估算：`1250万 × 5 gwei ≈ 0.00625 BNB ≈ 2-4 美元`

**稳妥建议部署钱包①准备 0.1 BNB（约 50-70 美元）**，足够部署全部 + 部分后续操作。
> 若另加 PancakeSwap 初始底池（200万 ACE），需额外 USDT 配对 + 少量 BNB，不计入上面。

### （用户确认）底池延后加
- **部署阶段不加底池**。当前只部署合约，不注入 PancakeSwap 流动性。
- 何时加底池由用户决定（需要开放 ACE 自由买卖交易时再加）。
- 加底池需额外 USDT 配对 + 少量 BNB，届时另行准备执行。

---

## 七、用户待办（明天继续）

- [ ] 确认部署链：**先在 BSC 测试网（免费）跑通验证**，后上主网（推荐）
- [ ] 确认 1000 万 ACE 归属方案（A 改合约指定 owner / B 部署后转走）
- [ ] 创建 5 个钱包并回填地址（①部署 / ②资金 / ③公益 / ④平台 / ⑤底池）
- [ ] 给部署钱包① 充值约 0.1 BNB
- [ ] 部署工具根据地址参数生成部署交易，用户签名确认

---

*本文件为部署前准备文档，含地址方案；请勿将任何钱包私钥写入本仓库。*
