# 合约升级方案 - 按"方向/套餐"购买模型

> 创建：2026-08-04
> 状态：定稿，待实施
> 目的：将 Course 合约从"按courseId单门课买"升级为"按方向/套餐买"，并加入体验课。

---

## 一、产品与定价（最终确认）

| ID | 产品 | 价格 | 解锁内容 | ACE赠送 |
|----|------|------|---------|--------|
| P0 | 体验课 | 200U | 1节自制体验课(平台自制单节) | 少送/暂不送 |
| P1 | 单选方向 | 500U | 1个方向(用户选择) | 1:1 → 5000 ACE |
| P2 | 4个方向 | 1500U | 4个方向(用户选择) | 1:1.2 → 18000 ACE |
| P3 | 全部(至尊) | 5000U | 全部当前+未来新增,终身 | 1:1.5 → 75000 ACE |

## 二、方向定义

- 目前定义 **5个方向**：ai应用 / 跨境电商 / 区块链 / 数字营销 / 编程开发
- 每个方向可增（价格不变，解锁方向=该方向所有课，学完为止）
- 方向数量可扩展（addDirection）

## 三、ACE赠送换算（基于 ACE 初始价 0.1U）

- 500U × 1.0 / 0.1 = 5000 ACE
- 1500U × 1.2 / 0.1 = 18000 ACE
- 5000U × 1.5 / 0.1 = 75000 ACE

## 四、合约需新增

1. **方向结构** Direction { id, name, active } + addDirection
2. **套餐结构** Package { id, price, dirCount(0=至尊全部), allAccess, bonusMul, active }
3. **用户解锁记录**：
   - userDirections[wallet] → 已解锁方向ID数组
   - userAllAccess[wallet] → 是否至尊
   - userHasTrial[wallet] → 是否购体验课
   - unlockTrialCourse[wallet] → 体验课解锁标记
4. **购买函数**：
   - buyTrial() → 200U, 解锁体验课
   - buyDirection(directionId) → 500U, 解锁1方向
   - buyFourDirections(d1,d2,d3,d4) → 1500U, 解锁4方向
   - buyAllAccess() → 5000U, 全部+未来
5. **解锁判定**：
   - canAccessDirection(wallet, dirId) → bool
   - canAccessTrial(wallet) → bool
   - getUnlockedDirections(wallet) → 列表

## 五、保留原有逻辑

- USDT 收付、资金分配(_distributeFunds: 直推/底池20%/公益1%/学员ACE)
- 推荐关系 (Referral合约交互)
- 代币分阶段释放（Lock 合约：30%+30%+40%）
- 退课（refundCourse，只退ACE，examPassed不可退）
- owner 管理、白名单、转让所有权

## 六、实施步骤

1. 重写 2_CoursePurchase.sol（方向/套餐模型）
2. 编译验证
3. 重新部署 Course 合约（owner=部署①）
4. 初始化：评估方向5个 + 套餐4个 + 关联 Referral/Lock + 白名单 + 转owner资金②
5. 前端配合：规划师/课程库改为 体验课200/方向500/4方向1500/至尊5000 + 购买按套餐/方向
6. 更新 contracts-config 的 Course 地址

## 七、风险与注意

- 需重新部署 Course 合约（其余5个不动）
- 部署成本极低（~0.15 USD）
- 部署后旧 Course 地址作废，前端+其他合约引用需更新
- 重部署后需把 Course 地址重新关联到 Referral/Lock/Cert
- 原200U旧 buyCourse 逻辑废弃，改套餐模型

*本文档为唯一权威定价/逻辑来源，任何改动须同步更新合约+前端+本文档。*

---

## 八、部署执行流程（临时授权方案C）2026-08-04 补充

**前提**：新 Course v2 合约已写好、编译通过、ABI已更新、方案已归档。

**背景卡点**：Referral/Lock/Cert/Course 的 owner 当前=资金②(0x0CF5...6671)。要更新这些合约的 courseContract 指向新 Course，需 owner 操作。采用"临时授权给部署①"方案。

**用户操作（在电脑 bscscan 上，连接资金②，对4个合约各 transferOwnership 一次）**：
newOwner = 部署钱包① = 0xcbB8c62C2155A9eec61Dd5107A9827E3ccC21738
1. ACECourse(旧)  0xf3dEa9374Ba3Dd553D8B863c9De5ff32F548C0Aa → transferOwnership(newOwner)
2. ACEReferral    0xc7C5431353B83908d56841F4288b417bBCd32c66 → transferOwnership(newOwner)
3. ACELock        0x0f03dcBD35262954aAD74D963Acb01b9F8439B79 → transferOwnership(newOwner)
4. ACECertificate 0xd5AaC5e1c450F884983Ffd1F959A9dfE9cFde493 → transferOwnership(newOwner)

**工具自动(部署①做) 用户授权完成后**：
5. 部署新 Course v2，owner=部署①
6. 对新Course关联：setReferralContract(Referral旧地址)、setLockContract(Lock旧地址)
7. 更新 Referral/Lock/Cert 的 courseContract = 新Course地址（此时它们owner=部署①，工具能改）
8. ACEToken.setExemptFromFee(新Course, true)（owner=部署①?——ACEToken owner也是资金②，需确认授权或工具无法改，见备注）
9. 新Course已内置5方向+3套餐，无需额外add
10. 所有触发的合约 owner 转回资金②：新Course/referral/lock/cert

**备注**：
- ACEToken 的 setExemptFromFee(新Course,true) 也需要 ACEToken owner（资金②），若未授权给部署①，则需要资金②额外做1次 transferOwnership(ACEToken→部署①) 或在 bscscan 直接 setExemptFromFee。因此用户实际可能需要操作5次(含ACEToken)。
- 白名单豁免非必须（新Course转ACE到Lock再发学员,若ACE对Course收卖出费会出问题），建议务必放开。

**最终**：owner 全部回到资金②，新Course地址更新到 contracts-config.js，前端按 v2 方法(buyPackage等)改。
